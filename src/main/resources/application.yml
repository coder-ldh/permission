# 本地环境
server:
  port: 9999
  servlet:
    context-path: /
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.48.128/admin?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&zeroDateTimeBehavior=convertToNull
    username: root
    password: root

  # Redis数据库索引（默认为0）
  redis:
    host: 192.168.48.128
    port: 6379
#    timeout: ${redis.timeout}
#    lettuce:
#      pool:
#        max-active: ${redis.max-active}
#        max-idle: ${redis.max-idle}
#        max-wait: ${redis.max-wait}
#        min-idle: ${redis.min-idle}
#logging:
#  level: com.ldh.permission.core.aspect=debug
mybatis-plus:
  global-config:
    mapper-locations: classpath:com/ldh/permission/core/mapper/*Mapper.xml
    #主键类型  0:"数据库ID自增", 1:"用户输入ID",2:"全局唯一ID (数字类型唯一ID)", 3:"全局唯一ID UUID";
    type-aliases-package: com.ldh.permission.core.model
    id-type: 0
    #字段策略 0:"忽略判断",1:"非 NULL 判断"),2:"非空判断"
    field-strategy: 2
    #驼峰下划线转换
    db-column-underline: true
    #mp2.3+ 全局表前缀 mp_
    #table-prefix: mp_
    #刷新mapper 调试神器
    refresh-mapper: true
    #数据库大写下划线转换
    #capital-mode: true